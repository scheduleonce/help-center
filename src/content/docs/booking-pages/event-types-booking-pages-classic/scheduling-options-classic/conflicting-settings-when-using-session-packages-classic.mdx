---
title: "Conflicting settings when using Session packages [Classic]"
---

import { Aside } from '@astrojs/starlight/components';

Session packages are not compatible with the following configuration settings and vice versa:

## Limitation on the number of bookings per day or per week 

If you have defined any limitation on the number of bookings per day or week in the [Time slot settings](/introduction-to-time-slot-settings/ "Introduction to Time slot settings") of the Booking page, you cannot offer session packages through that Booking page. This is due to the fact that the limitation on the number of bookings per day or week takes into account booked appointments. 

Any limitations on the manner in which Session packages can be made must be done when you set up the Session package, before any bookings are submitted.

[Learn more about limiting on the number of bookings per day or per week](/limiting-the-number-of-bookings-per-day-or-per-week/ "Limiting the number of bookings per day or per week")

## Rule-based assignment Master pages

Session packages cannot be used with Master pages with Rule-based assignment. This is because Rule-based assignment bookings are distributed to Team members via [Round robin](/resource-pool-distribution-method---round-robin/ "Resource pool distribution method: Round robin"), [Pooled availability](/distribution-method---pooled-availability/ "Distribution method - Pooled availability"), or [Pooled availability with priority](/distribution-method---pooled-availability-with-priority/ "Distribution method - Pooled availability with priority"). If Session packages were enabled, the bookings might be split across multiple Team members. For this reason, it is not allowed.

[Learn more about Master pages with Rule-based assignment](/team-or-panel-page/ "Master page scenario: Rule-based assignment")