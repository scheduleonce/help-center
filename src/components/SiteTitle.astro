---
import { isScheduleOnce } from "~/utils/pathConditions";

const { siteTitleHref } = Astro.locals.starlightRoute;
const path = Astro.url.pathname;
const isSo = isScheduleOnce(path);
import { isHomepage } from "~/utils/pathConditions";
---

<a href={siteTitleHref} class="site-title sl-flex">
  {
    isSo ? (
      <img src="/assets/so-logo.png" alt="ScheduleOnce Help" class="so-logo" />
    ) : (
      <>
        <img
          src="/assets/oh-logo.svg"
          alt="OnceHub Logo"
          class="oh-logo oh-logo-light"
          width="150"
          height="41"
        />
        <img
          src="/assets/oh-logo-white.svg"
          alt="OnceHub Logo"
          class="oh-logo oh-logo-dark"
          width="150"
          height="41"
        />
        <span class="logo-text">{isHomepage(path) && "Help"}</span>
      </>
    )
  }
</a>

<style>
  @layer starlight.core {
    .site-title {
      align-items: center;
      /* gap: 8px; */
      font-size: 1.5rem;
      font-weight: 400;
      color: var(--sl-color-accent);
      text-decoration: none;
      white-space: nowrap;
      min-width: 0;
    }
    img {
      height: 40px;
      width: auto;
      max-width: 100%;
      object-fit: contain;
      object-position: 0 50%;
    }
    .logo-text {
      display: inline-block;
      font-size: 1.5rem;
      line-height: 1.875rem;
      font-weight: 400;
      color: #333;
      margin-top: 6px;
    }
    .oh-icon {
      height: 30px;
      width: 30px;
    }
    .oh-logo {
      height: 41px;
    }
    .oh-logo-dark {
      display: none;
    }
    :root[data-theme="dark"] .oh-logo-light {
      display: none;
    }
    :root[data-theme="dark"] .oh-logo-dark {
      display: inline-block;
    }
    :root[data-theme="dark"] .logo-text {
      color: #fff;
    }
    .so-logo {
      max-height: 40px;
      width: auto;
    }
  }
</style>
